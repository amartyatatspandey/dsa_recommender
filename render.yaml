services:
  - type: web
    name: dsa-recommender
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
    autoDeploy: true
    disk:
      name: data_disk
      mountPath: /opt/render/project/src/data
      sizeGB: 1
